import { AppBar, Toolbar, styled }  from '@mui/material';


const StyledHeader = styled(AppBar)'
  
'


const Header = () => {
    return (
        <AppBar>
            <Toolbar>

            </Toolbar>
        </AppBar>
    );
}

export default Header;